spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3309/crm_dev?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:adham}
    password: ${DB_PASSWORD:adham123}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jwt:
    secretKey: crm-dev-secret-key-change-in-production
    access-token-ttl-minutes: 15

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE